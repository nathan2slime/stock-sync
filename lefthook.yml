pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: pnpm lint

    - name: format check
      run: pnpm format:check

commit-msg:
  jobs:
    - name: commitlint
      run: pnpm commitlint --edit {1}
