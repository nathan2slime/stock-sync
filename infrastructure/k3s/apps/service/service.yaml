apiVersion: v1
kind: Service

metadata:
  name: stock-sync-service
  labels:
    app.kubernetes.io/name: stock-sync-service
    app.kubernetes.io/part-of: stock-sync

spec:
  selector:
    app.kubernetes.io/name: stock-sync-service

  ports:
    - name: http
      port: 80
      targetPort: http
